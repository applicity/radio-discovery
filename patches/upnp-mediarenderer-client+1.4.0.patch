diff --git a/node_modules/upnp-mediarenderer-client/index.js b/node_modules/upnp-mediarenderer-client/index.js
index d4c90b1..eb2497b 100644
--- a/node_modules/upnp-mediarenderer-client/index.js
+++ b/node_modules/upnp-mediarenderer-client/index.js
@@ -304,8 +304,19 @@ function buildMetadata(metadata) {
     res.text = metadata.subtitlesUrl;
   }
 
+  // Handle generic properties
+  ['song', 'dc', 'upnp'].forEach(k => {
+    if (metadata[k]) {
+      const md = metadata[k]
+      Object.keys(md).forEach(k2 => {
+        const i = et.SubElement(item, `${k}:${k2}`)
+        i.text = md[k2]
+      })
+    }
+  })
+
   var doc = new et.ElementTree(didl);
-  var xml = doc.write({ xml_declaration: false });
+  var xml = doc.write({ xml_declaration: true });
 
   return xml;
 }
